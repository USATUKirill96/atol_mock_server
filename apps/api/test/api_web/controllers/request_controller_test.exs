defmodule ApiWeb.PageControllerTest do
  use ApiWeb.ConnCase

  # TODO: Написать тесты контроллеров
#  test "GET /", %{conn: conn} do
#    conn = get(conn, "/")
#    assert html_response(conn, 200) =~ "Welcome to Phoenix!"
#  end
end
